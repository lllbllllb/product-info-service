server:
  shutdown: graceful

spring:
  codec:
    max-in-memory-size: 1MB
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/productinfoservice
    username: postgres
    password: password
  liquibase:
    url: jdbc:postgresql://localhost:5433/productinfoservice
    user: postgres
    password: password
